import { ICoreZIndex } from "./coreTypes";

/**
 * Z-index tokens. Resolved values: level0=0, level1=1, level2=2, level3=3, level99=99
 * @see ThemeReference.zIndex - hover for actual resolved values
 */
export interface ISystemZIndex extends ICoreZIndex {}
