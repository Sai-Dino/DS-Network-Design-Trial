/**
 * A placeholder index.ts.
 * We want to export all the sub modules within the core folder as independent path imports
 * So that the consumers of each of the sub module doesn't need to have anything to do with the other sub modules' code.
 * But precontruct needs a default main entrypoint for each of the packages. Hence this empty placeholder index.ts entrypoint.
 */
export {};
